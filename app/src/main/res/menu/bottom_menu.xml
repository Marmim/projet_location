<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

<item android:id="@+id/nav_home"
    android:icon="@drawable/baseline_home_filled_24"
    tools:ignore="MenuTitle"
    android:actionLayout="@layout/activity_profil_locataire"
    android:title="Home"/>

    <item android:id="@+id/search"
        android:icon="@drawable/baseline_search_24"
        tools:ignore="MenuTitle"
        android:title="Recherche" />

    <item android:id="@+id/favoris"
        android:icon="@drawable/baseline_favorite_24"
        tools:ignore="MenuTitle"
        android:title="Favoris" />

    <item android:id="@+id/notif"
        android:icon="@drawable/baseline_notifications_24"
        tools:ignore="MenuTitle"
        android:title="Notifications"/>

    <item android:id="@+id/profilmenu"
        android:icon="@drawable/femalepro"
        tools:ignore="MenuTitle"
        android:title="Profil"/>
</menu>